import React from 'react';
import { ReactComponent as FibusinessIconSvg } from '../icons/Fibusiness.svg';

const FibusinessIcon = (props) => (
  <FibusinessIconSvg {...props} />
);

export default FibusinessIcon;