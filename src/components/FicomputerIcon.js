import React from 'react';
import { ReactComponent as FicomputerIconSvg } from '../icons/Ficomputer.svg';

const FicomputerIcon = (props) => (
  <FicomputerIconSvg {...props} />
);

export default FicomputerIcon;