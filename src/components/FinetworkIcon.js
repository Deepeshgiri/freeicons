import React from 'react';
import { ReactComponent as FinetworkIconSvg } from '../icons/Finetwork.svg';

const FinetworkIcon = (props) => (
  <FinetworkIconSvg {...props} />
);

export default FinetworkIcon;