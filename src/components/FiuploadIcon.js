import React from 'react';
import { ReactComponent as FiuploadIconSvg } from '../icons/Fiupload.svg';

const FiuploadIcon = (props) => (
  <FiuploadIconSvg {...props} />
);

export default FiuploadIcon;